doctype html
html
    head
        link(rel="stylesheet" href="style.css")
        style. 
            p {
                color: red;
                text-decoration: underline;
            }
    body
        - const pStyles = {"text-align": "right", "text-transform": "lowercase"}
        p this is an HTML file
        p(style="text-align: center; text-transform: uppercase;") another example
        p(style=pStyles) a third example
        
