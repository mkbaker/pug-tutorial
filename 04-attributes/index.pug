doctype html 
html
    head
    body
        input(type="password" name="inpPwd" data-js=`${ 5 > 2 ? "OK" : "NOT OK" }`)
        p The above example is how to add attributes and some js logic
        p Use - to write a javascript line
        - const myClasses = ['class1', 'class2', 'class3']
        div(class=myClasses) You can do multiple classes easily like this
        div.my-div(class=myClasses) You can also do this

        - const myStyles = { "color": "red", "background-color": "blue" }
        div(style=myStyles) wow check out these inline styles

        - const myAttributes = { "src": "myPhoto.png", "alt":"This is my photo"}
        img&attributes(myAttributes)

        input(type="text" disabled)